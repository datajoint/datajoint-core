name: coverage

on: [push]
jobs:
  test:
    name: coverage
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      # - name: install tarpaulin
      #   uses: actions-rs/cargo@v1
      #   with:
      #     command: install
      #     args: >
      #       cargo-tarpaulin

      # - name: Install docker-compose
      #   run: |
      #     curl -L "https://github.com/docker/compose/releases/download/1.29.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
      #     chmod +x /usr/local/bin/docker-compose

      - name: Start test databases
        run: |
          cd packages/datajoint-core/tests
          ls
          pwd
          docker-compose -f docker-compose-test.yml run -d --name postgres_13 postgres_13
          docker exec postgres_13 bash -c "until pg_isready; do sleep 1; done"
          docker-compose -f docker-compose-test.yml run -d --name mysql_5_6
          run sleep 60

      # - name: Run tests and upload coverage
      #   run: |
      #     cargo tarpaulin --verbose --coveralls ${{ secrets.GITHUB_TOKEN }}

      - name: run tests
        run: |
          cargo test

      - name: clean
        run: |
          docker-compose -f docker-compose-test.yml rm

      #   are we going to upload these any where?
      # - name:                   Upload to codecov.io
      #   uses:                   codecov/codecov-action@v2
      #   with:
      #     # token:                ${{secrets.CODECOV_TOKEN}} # not required for public repos
      #     fail_ci_if_error:     true
